# CAD Model Drop Zone

Place your exported robot CAD model here as:

- `public/models/robot-arm.glb`

Recommended export settings:

- Use glTF binary (`.glb`)
- Keep object and joint names consistent with your CAD assembly hierarchy
- Apply real-world scale before export
